{
  "name": "@eclipsesource/uml-glsp",
  "version": "1.0.0",
  "description": "Sprotty diagrams for UML",
  "keywords": [
    "sprotty",
    "workflow",
    "diagram"
  ],
  "homepage": "https://github.com/eclipsesource/uml-glsp",
  "bugs": "https://github.com/eclipsesource/uml-glsp/issues",
  "repository": {
    "type": "git",
    "url": "https://github.com/eclipsesource/uml-glsp.git"
  },
  "license": "(EPL-2.0 OR MIT)",
  "author": {
    "name": "EclipseSource"
  },
  "files": [
    "lib",
    "src",
    "css",
    "images"
  ],
  "scripts": {
    "build": "npx tsc -b && yarn run lint",
    "clean": "npx rimraf lib tsconfig.tsbuildinfo",
    "icons": "npx ts-node ./scripts/icons.ts",
    "lint": "npx eslint -c ./.eslintrc.js --ext .ts,.tsx ./src",
    "lint:fix": "npx eslint --fix -c ./.eslintrc.js --ext .ts,.tsx ./src",
    "prepare": "yarn run clean && yarn run build",
    "watch": "npx tsc -w"
  },
  "dependencies": {
    "@eclipse-glsp/client": "next",
    "balloon-css": "^0.5.0",
    "reflect-metadata": "^0.1.10"
  },
  "devDependencies": {
    "endent": "^2.1.0"
  }
}
