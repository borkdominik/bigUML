package com.eclipsesource.uml.modelserver.commands.statemachinediagram.finalstate;

public class RemoveFinalStateShapeCommand { /*- {

    protected final Shape shapeToRemove;

    public RemoveFinalStateShapeCommand(final EditingDomain domain, final URI modelUri, final String semanticProxyUri) {
        super(domain, modelUri);
        this.shapeToRemove = UmlNotationCommandUtil.getNotationElement(modelUri, domain, semanticProxyUri, Shape.class);
    }

    @Override
    protected void doExecute() {
        umlDiagram.getElements().remove(shapeToRemove);
    }   */
}
