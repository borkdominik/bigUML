package com.eclipsesource.uml.modelserver.old.diagram.activitydiagram.partition;

public class RemovePartitionShapeCommand { /*-

   protected final Shape shapeToRemove;

   public RemovePartitionShapeCommand(final EditingDomain domain, final URI modelUri,
      final String semanticProxyUri) {
      super(domain, modelUri);
      this.shapeToRemove = UmlNotationCommandUtil.getNotationElement(modelUri, domain, semanticProxyUri, Shape.class);
   }

   @Override
   protected void doExecute() {
      umlDiagram.getElements().remove(shapeToRemove);
   }
      */
}
